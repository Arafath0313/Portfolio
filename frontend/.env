# ─── Local Development ─────────────────────────────────────────────────────────
# Set the bare Railway/localhost host — NO trailing slash, NO /api/v1
# /api/v1 is appended automatically by environment.js (VERSION field)
VITE_API_BASE_URL=http://localhost:8080
