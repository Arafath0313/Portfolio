spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/portfolio_db}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:Arafath0313}

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}

  servlet:
    multipart:
      enabled: true
      max-file-size: ${MULTIPART_MAX_FILE_SIZE:10MB}
      max-request-size: ${MULTIPART_MAX_REQUEST_SIZE:20MB}

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:arafathm0307@gmail.com}
    password: ${MAIL_PASSWORD:bjhm cbra clpi zvva}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    default-encoding: UTF-8

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:9M5qK7rL2xN8vB4pF1sW6eT3yU9iA0dH7jC5kL8mN2pQ4rS6tV1xY3zB5cD7eF9g}
  expiration: ${JWT_EXPIRATION:86400000}

portfolio:
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin123}
    email: ${ADMIN_EMAIL:admin@example.com}

storage:
  # On Railway: mount a Volume at /data/uploads and set STORAGE_UPLOAD_DIR=/data/uploads
  # Local dev:  set STORAGE_UPLOAD_DIR=uploads  (relative, resolved to CWD/uploads)
  upload-dir: ${STORAGE_UPLOAD_DIR:/data/uploads}

  allowed-image-types:
    - image/jpeg
    - image/png
    - image/webp
    - image/x-icon
    - image/vnd.microsoft.icon

  allowed-document-types:
    - application/pdf

  allowed-image-extensions:
    - jpg
    - jpeg
    - png
    - webp
    - ico

  allowed-document-extensions:
    - pdf

mail:
  from: ${MAIL_FROM:arafathm0307@gmail.com}
  owner: ${MAIL_OWNER:arafathm0307@gmail.com}

logging:
  file:
    name: logs/portfolio.log
  level:
    com.arafath.portfolio: DEBUG
    org.springframework.retry: DEBUG
    org.springframework.mail: DEBUG